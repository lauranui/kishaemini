import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.kishaemine_fanapp.app',
  appName: 'Kisha e Mine Fan App',
  webDir: 'dist'
};

export default config;
